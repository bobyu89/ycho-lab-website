# 國科會著作／計畫自動同步 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 國科會研究人才網的著作與計畫清單自動同步到實驗室網站，統一格式渲染，每週自動更新。

**Architecture:** GitHub Actions 每週跑 PowerShell 腳本抓國科會兩頁 → 解析成 `data/nstc.json` → 有變動才 commit（觸發 GitHub Pages 重新部署）。前端以共用渲染模組讀 JSON＋手工補充檔（徽章、非國科會計畫），統一模板渲染 publications / projects / 首頁統計。

**Tech Stack:** PowerShell（5.1 與 pwsh 皆需可跑）、GitHub Actions、vanilla JS（無框架、無建置步驟）、GitHub Pages。

**Spec:** `docs/superpowers/specs/2026-07-13-nstc-sync-design.md`

## Global Constraints

- 本機無 Node/Python；一切腳本用 PowerShell，且須同時相容 Windows PowerShell 5.1 與 pwsh 7（GitHub runner）。
- 前端維持現有模式：無建置步驟、fetch 失敗必須 fallback 到內建資料（比照 `js/data.js` 的 news/events）。
- 輸出格式一律統一：國科會與手工資料經同一模板渲染，頁面上不可看出來源差異。
- 樣式沿用既有 class（`.pub-block`、`.pub-list`、`.pub-badges`、`.pub-badge rank/role/grant`、`.glass.project-card`、`.project-tag`），不新增 CSS。
- 解析 0 筆時腳本以非零碼結束且不寫檔。
- git 分支為 `master`，push 即部署（GitHub Pages）。
- 預覽：`powershell tools/serve.ps1`，網址 `http://localhost:8788/`（埠易撞，被占用時換埠 `-Port 8790`）。
- commit 訊息結尾加 `Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>`。

## 資料來源（驗證過：純 GET、UTF-8、HTML 表格）

- 著作：`https://arspb.nstc.gov.tw/NSCWebFront/modules/talentSearch/talentSearch.do?action=initRsm05&rsNo=d224e67d09bd45b9b822380b65c26085&LANG=chi`
  - 資料表為頁面中**不帶屬性**的 `<table>`（另一個是 `<table class="layout">`），表頭列含 `<th>`：出版年月｜著作類別｜著作名稱｜作者｜收錄出處。目前 16 筆。
  - 儲存格內有大量空白與換行（標題也可能含換行）、著作類別與收錄出處埋在多層空白中，需 strip 標籤＋壓縮空白。
  - 作者格式不一（`Ho YC, Fang CL` 或 `Yen-Chung Ho, Chien Mei Sung`），照原樣保留。
- 計畫：同網址 `action=initRsm17new`。欄位：年度｜補助類別｜學門代碼｜計畫名稱｜擔任工作｜核定經費(新台幣)。目前 7 筆（含出席會議、短訪等小額補助，全部顯示）。
  - 經費在 `<td align="right"><span style="float:right;">1,092,000</span></td>` 內。
  - 補助類別可能兩行（`專題研究計畫` ＋ `(新進人員研究計畫)`），壓縮空白後會變成 `專題研究計畫 (新進人員研究計畫)`。

---

### Task 1: 同步腳本 `tools/sync-nstc.ps1` ＋ 產出 `data/nstc.json`

**Files:**
- Create: `tools/sync-nstc.ps1`
- Create: `data/nstc.json`（由腳本產出後 commit）

**Interfaces:**
- Produces: `data/nstc.json`，結構
  `{ lastSync: "YYYY-MM-DD", publications: [{date,type,title,authors,venue}], projects: [{year,category,field,title,role,budget}] }`
  全部欄位皆字串；後續任務（FALLBACK_NSTC、渲染）依賴此結構。

- [ ] **Step 1: 寫入腳本**

```powershell
# tools/sync-nstc.ps1 — 抓取國科會研究人才網著作/計畫，輸出 data/nstc.json
# 用法：powershell tools/sync-nstc.ps1  （或 pwsh，GitHub Actions 亦用此腳本）
# 結束碼：0=成功或無變動；1=抓取/解析失敗（不寫檔，網站維持原狀）
param(
  [string]$OutFile = (Join-Path (Split-Path -Parent $PSScriptRoot) "data/nstc.json")
)
$ErrorActionPreference = "Stop"
[Net.ServicePointManager]::SecurityProtocol = [Net.SecurityProtocolType]::Tls12

$RS_NO = "d224e67d09bd45b9b822380b65c26085"
$BASE  = "https://arspb.nstc.gov.tw/NSCWebFront/modules/talentSearch/talentSearch.do"

function Get-Page([string]$Url) {
  $resp = Invoke-WebRequest -Uri $Url -UseBasicParsing -UserAgent "Mozilla/5.0"
  # 一律以 UTF-8 解碼原始位元組，避免 PS5.1 依 header 誤判編碼
  $bytes = $resp.RawContentStream.ToArray()
  return [Text.Encoding]::UTF8.GetString($bytes)
}

function Get-CellText([string]$CellHtml) {
  $text = $CellHtml -replace '(?s)<[^>]+>', ' '
  $text = [Net.WebUtility]::HtmlDecode($text)
  return ($text -replace '\s+', ' ').Trim()
}

function Get-DataRows([string]$Html) {
  # 資料表 = 不帶屬性的 <table>，第一列是 <th> 表頭
  $table = [regex]::Match($Html, '(?s)<table>\s*<tr>\s*<th.*?</table>')
  if (-not $table.Success) { return @() }
  $rows = @()
  foreach ($tr in [regex]::Matches($table.Value, '(?s)<tr>(.*?)</tr>')) {
    if ($tr.Value -match '<th') { continue }
    $cells = @()
    foreach ($td in [regex]::Matches($tr.Groups[1].Value, '(?s)<td[^>]*>(.*?)</td>')) {
      $cells += Get-CellText $td.Groups[1].Value
    }
    if ($cells.Count -gt 0) { $rows += , $cells }
  }
  return , $rows
}

try {
  $pubHtml  = Get-Page "$BASE`?action=initRsm05&rsNo=$RS_NO&LANG=chi"
  $projHtml = Get-Page "$BASE`?action=initRsm17new&rsNo=$RS_NO&LANG=chi"
} catch {
  Write-Host "抓取失敗：$($_.Exception.Message)"
  exit 1
}

$publications = @()
foreach ($cells in (Get-DataRows $pubHtml)) {
  if ($cells.Count -lt 5) { continue }
  $publications += [ordered]@{
    date = $cells[0]; type = $cells[1]; title = $cells[2]; authors = $cells[3]; venue = $cells[4]
  }
}

$projects = @()
foreach ($cells in (Get-DataRows $projHtml)) {
  if ($cells.Count -lt 6) { continue }
  $projects += [ordered]@{
    year = $cells[0]; category = $cells[1]; field = $cells[2]
    title = $cells[3]; role = $cells[4]; budget = $cells[5]
  }
}

if ($publications.Count -eq 0 -or $projects.Count -eq 0) {
  Write-Host "解析 0 筆（著作 $($publications.Count)、計畫 $($projects.Count)），可能國科會改版，不更新。"
  exit 1
}

# 內容無變動就不寫檔（避免每週無意義 commit；lastSync 不列入比較）
$coreNew = [ordered]@{ publications = $publications; projects = $projects } | ConvertTo-Json -Depth 5
if (Test-Path $OutFile) {
  try {
    $old = Get-Content $OutFile -Raw -Encoding UTF8 | ConvertFrom-Json
    $coreOld = [ordered]@{ publications = $old.publications; projects = $old.projects } | ConvertTo-Json -Depth 5
    if ($coreOld -eq $coreNew) { Write-Host "資料無變動，不更新。"; exit 0 }
  } catch { }
}

$payload = [ordered]@{
  lastSync     = (Get-Date -Format "yyyy-MM-dd")
  publications = $publications
  projects     = $projects
} | ConvertTo-Json -Depth 5
New-Item -ItemType Directory -Force (Split-Path -Parent $OutFile) | Out-Null
[IO.File]::WriteAllText($OutFile, $payload, (New-Object Text.UTF8Encoding($false)))
Write-Host "已更新 $OutFile（著作 $($publications.Count) 筆、計畫 $($projects.Count) 筆）"
```

- [ ] **Step 2: 執行並驗證產出**

Run: `powershell -NoProfile -File tools/sync-nstc.ps1`
Expected: `已更新 ...data/nstc.json（著作 16 筆、計畫 7 筆）`（筆數以國科會當下為準，兩者皆 >0）

- [ ] **Step 3: 抽查 JSON 內容**

Run: `powershell -NoProfile -Command "$d = Get-Content data/nstc.json -Raw -Encoding UTF8 | ConvertFrom-Json; $d.publications[0]; $d.projects[0]; $d.lastSync"`
Expected: 第一筆著作含 date/type/title/authors/venue、第一筆計畫含 year/category/field/title/role/budget，值無 HTML 標籤與多餘空白（例：category 為 `短訪計畫 (邀請國際科技人士短期訪問)`、budget 為 `29,090`）。

- [ ] **Step 4: 驗證「無變動不更新」**

Run: 再跑一次 `powershell -NoProfile -File tools/sync-nstc.ps1`
Expected: 輸出 `資料無變動，不更新。`，exit code 0，檔案 mtime 不變。

- [ ] **Step 5: 確認 .gitignore 未擋住 data/**

Run: `git check-ignore data/nstc.json`；Expected: 無輸出（exit 1 = 未被忽略）。若被忽略，調整 `.gitignore`。

- [ ] **Step 6: Commit**

```
git add tools/sync-nstc.ps1 data/nstc.json
git commit -m "feat: 國科會資料同步腳本與首份 nstc.json"
```

---

### Task 2: GitHub Actions 排程 `.github/workflows/sync-nstc.yml`

**Files:**
- Create: `.github/workflows/sync-nstc.yml`

**Interfaces:**
- Consumes: `tools/sync-nstc.ps1`（exit 0=成功/無變動、exit 1=失敗）
- Produces: 每週一 08:00（台北）自動 commit `data/nstc.json`（有變動時）

- [ ] **Step 1: 寫入 workflow**

```yaml
name: Sync NSTC data
on:
  schedule:
    - cron: "0 0 * * 1"   # 每週一 00:00 UTC = 台北 08:00
  workflow_dispatch: {}     # GitHub 網頁可手動觸發
permissions:
  contents: write
jobs:
  sync:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Run sync script
        shell: pwsh
        run: ./tools/sync-nstc.ps1
      - name: Commit if changed
        shell: bash
        run: |
          if [ -n "$(git status --porcelain data/nstc.json)" ]; then
            git config user.name "github-actions[bot]"
            git config user.email "github-actions[bot]@users.noreply.github.com"
            git add data/nstc.json
            git commit -m "chore: sync NSTC data $(date +%F)"
            git push
          else
            echo "No changes."
          fi
```

- [ ] **Step 2: 本機驗證 YAML 可解析**

Run: `powershell -NoProfile -Command "Get-Content .github/workflows/sync-nstc.yml -Raw | Out-Null; 'ok'"`（僅確認檔案存在；語法正確性於 Task 7 push 後在 GitHub Actions 頁面手動觸發驗證）

- [ ] **Step 3: Commit**

```
git add .github/workflows/sync-nstc.yml
git commit -m "feat: 每週自動同步國科會資料的 GitHub Actions"
```

---

### Task 3: 手工補充檔 `js/enrich.js`

**Files:**
- Create: `js/enrich.js`

**Interfaces:**
- Produces（全域常數，供 nstc-render.js 與 index.html 使用）:
  - `PUB_BADGES: [{ match: string, badges: [{type: "rank"|"role"|"grant", text: string}] }]` — 以標題子字串（不分大小寫）比對
  - `PROJECT_EXTRAS: [{ match: string, year?: string, period?: string, desc?: string }]` — 幫國科會計畫補期間/描述；`year` 用於區分標題互為前綴的計畫
  - `EXTRA_PROJECTS: [{ year, sponsor, category, title, role, budget, period, desc }]` — 非國科會計畫（欄位皆字串，可空）

- [ ] **Step 1: 寫入檔案**（徽章內容自現行 publications.html 原樣搬入；補充描述自現行 projects.html 搬入）

```js
/* 手工補充資料（人工維護，改完 push 即上線）：
   - PUB_BADGES：論文徽章。match 填「該論文標題的一小段獨特文字」（不分大小寫）。
     type 可用 rank（排名）、role（作者角色）、grant（計畫編號）。
   - PROJECT_EXTRAS：幫國科會計畫補中文描述與執行期間。match 比對計畫名稱；
     若兩個計畫名稱很像，加 year（民國年度）區分。
   - EXTRA_PROJECTS：國科會以外的計畫（經濟部、醫院、校內計畫…），手工全欄位維護。
   詳見 README-維護說明.md */

const PUB_BADGES = [
  { match: "Cross-Device Platform", badges: [
    { type: "rank", text: "Scopus 235/668 General Medicine" },
    { type: "role", text: "第一作者・通訊作者" },
    { type: "grant", text: "NSTC 114-2314-B-468-006" }
  ]},
  { match: "AI Virtual Human", badges: [
    { type: "rank", text: "Scopus 235/668 General Medicine" },
    { type: "role", text: "第一作者" },
    { type: "grant", text: "教育部 PEH1140261" }
  ]},
  { match: "Prevalence and Correlates", badges: [
    { type: "rank", text: "SCIE 157/322 Oncology" },
    { type: "grant", text: "NSTC 108-2314-B-038-123" }
  ]},
  { match: "cut-off values", badges: [
    { type: "rank", text: "SSCI 35/144 Psychiatry" },
    { type: "role", text: "第一作者" }
  ]},
  { match: "seclusion and restraint", badges: [
    { type: "rank", text: "SSCI 2/123 Nursing" }
  ]},
  { match: "Identifying differential trajectories", badges: [
    { type: "rank", text: "SSCI 17/77 Psychology, Developmental" },
    { type: "role", text: "第一作者" }
  ]},
  { match: "life stress, smoking behavior", badges: [
    { type: "rank", text: "SSCI 22/123 Nursing" },
    { type: "role", text: "第一作者" }
  ]},
  { match: "External Counterpulsation", badges: [
    { type: "role", text: "通訊作者" }
  ]},
  { match: "Anesthetic Care Experience", badges: [
    { type: "role", text: "通訊作者" }
  ]},
  { match: "Correlations between Anhedonia", badges: [
    { type: "role", text: "第一作者・通訊作者" }
  ]},
  { match: "Chinese version", badges: [
    { type: "role", text: "第一作者・通訊作者" }
  ]},
  { match: "Differential trajectory and determinants", badges: [
    { type: "role", text: "第一作者・通訊作者" }
  ]}
];

const PROJECT_EXTRAS = [
  { match: "失樂感個人化動態監測", year: "114",
    period: "114/08/01–115/07/31",
    desc: "以精準及預防精神醫學為方向，結合移動平均理論與生態瞬時評估，發展跨裝置失樂感趨勢視覺化與個人化監測方法。" },
  { match: "失樂感個人化動態監測", year: "115",
    period: "",
    desc: "第二、三年延續計畫：持續發展跨裝置失樂感趨勢視覺化與個人化動態監測系統。" },
  { match: "穿戴式AR與VR", year: "114",
    period: "114/01/01–114/12/31",
    desc: "結合沉浸式科技與健康教育，評估青少年菸害防制介入教材之學習成效。" }
];

const EXTRA_PROJECTS = [
  { year: "115", sponsor: "經濟部產業發展署／資策會", category: "產業研究案",
    title: "照護產業科技化指數與指引研究案", role: "共同主持人", budget: "",
    period: "115/01/01–115/12/31",
    desc: "建立照護產業科技化評估指標與推動指引，支援長照與照護服務數位轉型。" },
  { year: "114", sponsor: "三軍總醫院", category: "擬真教育研究",
    title: "氣管內管置放處置擬真教育成效與學習反應異質性分析", role: "研究督導", budget: "",
    period: "112–114",
    desc: "以 responder analysis 與 mixed-methods 評估擬真訓練中誰最受益，並提出分層教學設計。" },
  { year: "114", sponsor: "亞洲大學", category: "教學實踐研究",
    title: "AI 虛擬人增強遊戲式教學與護理學生情緒智能", role: "", budget: "",
    period: "",
    desc: "依據 JMIR Research Protocols 發表之研究設計，評估 AI 虛擬人結合遊戲式教學對護理學生情緒智能之教學成效。" }
];
```

- [ ] **Step 2: 核對 match 關鍵字與 nstc.json 一對一對應**

Run: `powershell -NoProfile -Command "$d = Get-Content data/nstc.json -Raw -Encoding UTF8 | ConvertFrom-Json; foreach ($m in @('Cross-Device Platform','AI Virtual Human','Prevalence and Correlates','cut-off values','seclusion and restraint','Identifying differential trajectories','life stress, smoking behavior','External Counterpulsation','Anesthetic Care Experience','Correlations between Anhedonia','Chinese version','Differential trajectory and determinants')) { $hits = @($d.publications | Where-Object { $_.title -match [regex]::Escape($m) -or $_.title.ToLower().Contains($m.ToLower()) }); \"$($hits.Count)`t$m\" }"`
Expected: 每列都是 `1<tab>關鍵字`。若某列為 0 或 ≥2，回到 nstc.json 找該論文實際標題，改 match 為更獨特的子字串後重跑。

- [ ] **Step 3: Commit**

```
git add js/enrich.js
git commit -m "feat: 手工補充檔（論文徽章、非國科會計畫、計畫描述）"
```

---

### Task 4: `js/data.js` 加 fetchNstc/FALLBACK_NSTC/escapeHtml；首頁統計自動化

**Files:**
- Modify: `js/data.js`（檔尾附加）
- Modify: `index.html`（統計區加 id、boot 加統計計算、移除內嵌 escapeHtml、載入 enrich.js）

**Interfaces:**
- Consumes: `data/nstc.json`（Task 1）、`EXTRA_PROJECTS`（Task 3）
- Produces:
  - `async function fetchNstc(): Promise<{lastSync, publications[], projects[]}>`（失敗回 FALLBACK_NSTC）
  - `const FALLBACK_NSTC = {...}`（內建快照）
  - `function escapeHtml(str): string`（全站共用，自 index.html 內嵌搬入）

- [ ] **Step 1: data.js 檔尾加入**（`FALLBACK_NSTC` 的值＝把 `data/nstc.json` 的完整內容原樣貼進來——JSON 是合法的 JS 物件字面值）

```js
/* 國科會研究人才網同步資料（data/nstc.json 由 tools/sync-nstc.ps1 每週自動更新）。
   讀取失敗時顯示以下內建快照。 */
const FALLBACK_NSTC = /* ← 貼上 data/nstc.json 全文 */;

async function fetchNstc() {
  try {
    const res = await fetch("data/nstc.json", { cache: "no-cache" });
    if (!res.ok) throw new Error(`nstc http ${res.status}`);
    const data = await res.json();
    if (!data.publications || !data.publications.length) throw new Error("empty nstc data");
    return data;
  } catch {
    return FALLBACK_NSTC;
  }
}

function escapeHtml(str) {
  return String(str).replace(/[&<>"']/g, (c) =>
    ({ "&": "&amp;", "<": "&lt;", ">": "&gt;", '"': "&quot;", "'": "&#39;" }[c]));
}
```

- [ ] **Step 2: index.html — 刪除內嵌 escapeHtml**

刪除 index.html 內嵌 script 中的（data.js 已提供同名函式）：

```js
    function escapeHtml(str) {
      return String(str).replace(/[&<>"']/g, (c) =>
        ({ "&": "&amp;", "<": "&lt;", ">": "&gt;", '"': "&quot;", "'": "&#39;" }[c]));
    }
```

- [ ] **Step 3: index.html — 統計區加 id**

三個 `data-count` span 改為（Lab Members 不動）：

```html
<div class="stat-num"><span id="stat-journal" data-count="7">0</span></div>
...
<div class="stat-num"><span id="stat-conf" data-count="5">0</span></div>
...
<div class="stat-num"><span id="stat-projects" data-count="4">0</span></div>
```

- [ ] **Step 4: index.html — 載入 enrich.js 並於 boot() 內計算統計**

`<script src="js/data.js"></script>` 之後加一行 `<script src="js/enrich.js"></script>`。
boot() 內、`initCounters();` 之前加入：

```js
      const nstc = await fetchNstc();
      const setCount = (id, n) => { const el = document.getElementById(id); if (el) el.dataset.count = String(n); };
      setCount("stat-journal", nstc.publications.filter((p) => p.type === "期刊論文").length);
      setCount("stat-conf", nstc.publications.filter((p) => p.type.includes("研討會")).length);
      setCount("stat-projects", nstc.projects.length + EXTRA_PROJECTS.length);
```

- [ ] **Step 5: 瀏覽器驗證**

啟動預覽（`powershell tools/serve.ps1`），開 `http://localhost:8788/`：
- 統計數字動畫跑完後為實際筆數（期刊 8、研討會 8、計畫 10；以 nstc.json 實際內容為準）
- 最新消息、重要活動、活動花絮照常渲染，console 無錯誤

- [ ] **Step 6: Commit**

```
git add js/data.js index.html
git commit -m "feat: fetchNstc 與首頁統計自動計算"
```

---

### Task 5: 渲染模組 `js/nstc-render.js` ＋ publications.html 改 JS 渲染

**Files:**
- Create: `js/nstc-render.js`
- Modify: `publications.html`（刪手寫 `<ol>`，改容器＋渲染）

**Interfaces:**
- Consumes: `fetchNstc()`、`escapeHtml()`（Task 4）、`PUB_BADGES`、`PROJECT_EXTRAS`、`EXTRA_PROJECTS`（Task 3）
- Produces（全域函式，projects.html 於 Task 6 也使用）:
  - `renderPublications(container: HTMLElement, data): void` — 依 type 分組渲染
  - `renderProjects(container: HTMLElement, data): void`
  - `renderSyncNote(el: HTMLElement, data): void` — 「資料來源：國科會研究人才網｜最後同步 YYYY-MM-DD」

- [ ] **Step 1: 寫入 js/nstc-render.js**

```js
/* 統一渲染模板：國科會同步資料與手工補充（enrich.js）都走這裡，
   輸出格式一律一致。publications.html 與 projects.html 共用。 */

function boldPI(escapedAuthors) {
  // 老師姓名各種寫法一律加粗：Yen-Chung Ho / Ho YC / Ho Y-C / Ho, Y.-C.
  return escapedAuthors.replace(
    /(Yen[-\s]?Chung\s+Ho|Ho\s*,?\s*Y\.?\s*-?\s*C(?:hung)?\.?(?=[,;\s)]|$)|Ho\s+YC\b)/g,
    "<strong>$1</strong>"
  );
}

function badgesFor(title) {
  const t = String(title).toLowerCase();
  const hit = (typeof PUB_BADGES !== "undefined" ? PUB_BADGES : [])
    .find((e) => t.includes(e.match.toLowerCase()));
  return hit ? hit.badges : [];
}

function pubItemHtml(p) {
  const year = String(p.date || "").slice(0, 4);
  const venue = p.venue ? ` <em>${escapeHtml(p.venue)}</em>.` : "";
  const badges = badgesFor(p.title);
  const badgeHtml = badges.length
    ? `<div class="pub-badges">${badges
        .map((b) => `<span class="pub-badge ${escapeHtml(b.type)}">${escapeHtml(b.text)}</span>`)
        .join("")}</div>`
    : "";
  return `<li>${boldPI(escapeHtml(p.authors))} (${escapeHtml(year)}). ${escapeHtml(p.title)}.${venue}${badgeHtml}</li>`;
}

function renderPublications(container, data) {
  const pubs = [...data.publications].sort((a, b) => String(b.date).localeCompare(String(a.date)));
  // 依類別分組：期刊論文、研討會論文固定順序在前，其他類別（若未來出現）依序附後
  const order = ["期刊論文", "研討會論文"];
  const groups = new Map(order.map((k) => [k, []]));
  pubs.forEach((p) => {
    const key = order.includes(p.type) ? p.type : p.type || "其他發表";
    if (!groups.has(key)) groups.set(key, []);
    groups.get(key).push(p);
  });
  container.innerHTML = "";
  groups.forEach((items, label) => {
    if (!items.length) return;
    const block = document.createElement("div");
    block.className = "pub-block reveal";
    block.innerHTML = `<h3>${escapeHtml(label)}</h3><ol class="pub-list">${items.map(pubItemHtml).join("")}</ol>`;
    container.append(block);
  });
}

function projectExtrasFor(proj) {
  const extras = typeof PROJECT_EXTRAS !== "undefined" ? PROJECT_EXTRAS : [];
  return extras.find((e) =>
    String(proj.title).includes(e.match) && (!e.year || e.year === proj.year)
  ) || {};
}

function projectCardHtml(p) {
  const extra = p.sponsor ? {} : projectExtrasFor(p); // 手工計畫（有 sponsor）自帶 desc/period
  const sponsor = p.sponsor || "國科會";
  const period = p.period || extra.period || (p.year ? `${p.year} 年度` : "");
  const desc = p.desc || extra.desc || "";
  const budget = p.budget ? `NT$ ${p.budget}` : "";
  const tag = [p.role, sponsor, p.category, period, budget].filter(Boolean).map(escapeHtml).join("｜");
  return `<article class="glass project-card reveal">
    <span class="project-tag">${tag}</span>
    <h3>${escapeHtml(p.title)}</h3>
    ${desc ? `<p>${escapeHtml(desc)}</p>` : ""}
  </article>`;
}

function renderProjects(container, data) {
  const extras = typeof EXTRA_PROJECTS !== "undefined" ? EXTRA_PROJECTS : [];
  const all = [...data.projects, ...extras].sort(
    (a, b) => (parseInt(b.year, 10) || 0) - (parseInt(a.year, 10) || 0)
  );
  container.innerHTML = all.map(projectCardHtml).join("");
}

function renderSyncNote(el, data) {
  if (!el) return;
  el.textContent = `資料來源：國科會研究人才網${data.lastSync ? `｜最後同步 ${data.lastSync}` : ""}`;
}
```

- [ ] **Step 2: publications.html 改容器＋渲染**

`<section class="section" ...>` 內的兩個手寫 `<div class="pub-block reveal">…</div>` 全部刪除，`<div class="container">` 內改為：

```html
        <div id="pub-groups"></div>
        <p class="mono-label" id="sync-note" style="margin-top:28px"></p>
```

頁尾 script 區改為：

```html
  <script src="js/icons.js"></script>
  <script src="js/components.js"></script>
  <script src="js/data.js"></script>
  <script src="js/enrich.js"></script>
  <script src="js/nstc-render.js"></script>
  <script src="js/animations.js"></script>
  <script>
    initLayout("publications");
    (async () => {
      const data = await fetchNstc();
      renderPublications(document.getElementById("pub-groups"), data);
      renderSyncNote(document.getElementById("sync-note"), data);
      initReveal();
    })();
  </script>
```

（hero 文案 `依目前教師網頁資料整理，含期刊論文與研討會論文。` 改為 `同步自國科會研究人才網，含期刊論文與研討會論文。`）

- [ ] **Step 3: 瀏覽器驗證**

開 `http://localhost:8788/publications.html`：
- 「期刊論文」「研討會論文」兩區塊都渲染、年月新→舊
- 老師姓名（Ho YC / Yen-Chung Ho / Ho Y-C 各種寫法）全部粗體
- 12 筆徽章正確掛上（對照舊版頁面）；國科會新增而未補徽章的（如 2026-07 心理劇、2026-02 老年友善照護）素面顯示
- venue 空的研討會論文不出現孤懸的句點或空 `<em>`
- 頁尾顯示「資料來源：國科會研究人才網｜最後同步 YYYY-MM-DD」
- console 無錯誤
- 故障演練：DevTools Network 設 offline 重新整理（或暫時把 fetch 網址改壞），確認 fallback 快照渲染相同內容

- [ ] **Step 4: Commit**

```
git add js/nstc-render.js publications.html
git commit -m "feat: 成果發表頁改為國科會資料驅動的統一模板渲染"
```

---

### Task 6: projects.html 改 JS 渲染

**Files:**
- Modify: `projects.html`（刪手寫卡片，改容器＋渲染）

**Interfaces:**
- Consumes: `fetchNstc()`、`renderProjects()`、`renderSyncNote()`、`EXTRA_PROJECTS`

- [ ] **Step 1: projects.html 改容器＋渲染**

`<div class="project-list" data-stagger>` 內的五張手寫 `<article>` 全部刪除，改為：

```html
        <div class="project-list" data-stagger id="project-list"></div>
        <p class="mono-label" id="sync-note" style="margin-top:28px"></p>
```

頁尾 script 區改為：

```html
  <script src="js/icons.js"></script>
  <script src="js/components.js"></script>
  <script src="js/data.js"></script>
  <script src="js/enrich.js"></script>
  <script src="js/nstc-render.js"></script>
  <script src="js/animations.js"></script>
  <script>
    initLayout("projects");
    (async () => {
      const data = await fetchNstc();
      renderProjects(document.getElementById("project-list"), data);
      renderSyncNote(document.getElementById("sync-note"), data);
      initReveal();
    })();
  </script>
```

- [ ] **Step 2: 瀏覽器驗證**

開 `http://localhost:8788/projects.html`：
- 國科會 7 筆＋手工 3 筆共 10 張卡片，年度新→舊混排
- 兩筆失樂感計畫各自掛到正確的描述（114 有期間、115 顯示「115 年度」與延續描述）
- 小額補助（出席會議 NT$ 50,000、短訪 NT$ 29,090、研究生出席 109 年度）正常顯示
- 非國科會計畫標籤顯示正確單位（經濟部產業發展署／資策會、三軍總醫院、亞洲大學），格式與國科會卡片一致
- console 無錯誤

- [ ] **Step 3: Commit**

```
git add projects.html
git commit -m "feat: 研究計畫頁改為國科會＋手工資料混排渲染"
```

---

### Task 7: README 維護說明、整站驗證、上線與雲端排程驗證

**Files:**
- Modify: `README-維護說明.md`（新增「國科會自動同步」章節）

**Interfaces:**
- Consumes: 前六個任務的全部產出

- [ ] **Step 1: README-維護說明.md 新增章節**

```markdown
## 國科會著作／計畫自動同步

- 成果發表、研究計畫、首頁統計的資料來源是 `data/nstc.json`，
  由 GitHub Actions 每週一早上 8 點（台北時間）自動從國科會研究人才網同步，有變動才更新上線。
- 想立刻同步：到 GitHub repo → Actions → 「Sync NSTC data」→ Run workflow。
  或本機執行 `powershell tools/sync-nstc.ps1` 後 push。
- 幫論文加徽章（排名／作者角色／計畫編號）、幫計畫加中文描述、
  新增國科會以外的計畫：編輯 `js/enrich.js`（檔內有格式說明），改完 push 即上線。
- 若國科會改版導致同步失敗：Actions 會顯示紅色失敗，網站維持原狀不受影響，
  屆時需調整 `tools/sync-nstc.ps1` 的解析規則。
```

- [ ] **Step 2: 整站瀏覽器回歸**

`http://localhost:8788/` 首頁、publications、projects、research、members、gallery、pi 各開一次：console 無錯誤、導覽正常、統計數字正確、手機寬度（375px）下兩個新頁面排版正常。

- [ ] **Step 3: Commit 並 push 上線**

```
git add README-維護說明.md
git commit -m "docs: 國科會自動同步維護說明"
git push
```

- [ ] **Step 4: 驗證線上版**

等 GitHub Pages 部署完成（約 1 分鐘），開 `https://bobyu89.github.io/ycho-lab-website/publications.html` 與 `projects.html`，確認與本機一致（特別確認 `data/nstc.json` 能被 Pages 正常供檔）。

- [ ] **Step 5: 手動觸發 workflow 驗證雲端排程**

GitHub repo → Actions → Sync NSTC data → Run workflow（master）。
Expected: job 綠色成功，log 顯示 `資料無變動，不更新。` ＋ `No changes.`（因剛同步過）。
（此步需要登入 GitHub 網頁，由使用者操作或在瀏覽器協助下進行。）

---

## Self-Review 紀錄

- Spec 覆蓋：同步腳本(T1)、排程(T2)、enrich(T3)、fetchNstc/fallback/統計(T4)、publications 統一模板(T5)、projects 混排(T6)、README/驗證/上線(T7)——spec 各節皆有對應任務。
- 錯誤處理四情境：0 筆→exit 1 不寫檔(T1)；無變動→不 commit(T1/T2)；前端失敗→FALLBACK_NSTC(T4)；徽章比對不到→素面(T5 badgesFor 回空陣列)。
- 型別/命名一致性：fetchNstc、escapeHtml、PUB_BADGES、PROJECT_EXTRAS、EXTRA_PROJECTS、renderPublications、renderProjects、renderSyncNote 在各任務間拼寫一致。
- 已知風險：PUB_BADGES 的 match 關鍵字以摘要資料撰寫，T3 Step 2 有一對一核對步驟兜底。
